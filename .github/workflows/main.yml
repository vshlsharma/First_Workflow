name: My first CI Pipeline concurrency

on:
 push:
  branches: [ main ]

jobs:
  build:
   runs-on: ubuntu-latest

   steps:
     - name: step1
       run: echo "Hi , Vishal this side "

     - name: step2
       run: echo "Hi this is another step 2"
     
       
